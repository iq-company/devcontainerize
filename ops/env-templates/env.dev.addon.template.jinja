
# --- Docker Image (DevContainer uses dev image) ---
IQ_IMAGE={{ image_prefix }}-dev
IQ_IMAGE_TAG=latest

# --- Dev-only Settings (container permissions & VSCode) ---
HOST_UID={% raw %}{{uid}}{% endraw %}
HOST_GID={% raw %}{{gid}}{% endraw %}
VSCODE_SETTINGS_PATH={% raw %}{{vscode_settings_path}}{% endraw %}
DEV_CONTAINER=1

# --- DevContainer Profiles ---
# db: Database container
# redis: Redis cache and queue
# (stack profile is NOT used in DevContainer - Procfile handles services)
COMPOSE_PROFILES=db,redis

