# Alpine variant configuration for dev image
# Use: bench ops dockerfile create -v alpine
# Note: Alpine uses WeasyPrint for PDF generation (wkhtmltopdf not available)

alpine_version: "{{ alpine_version }}"

# Alpine-specific package names differ from Debian
# These are handled by the recipes automatically
